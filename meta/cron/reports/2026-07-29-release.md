# ACL release hygiene — 2026-07-29

**Job:** `acl-release-hygiene` (`236df0509236`)  
**At:** 2026-07-29T13:00:39Z  
**Slot:** Wed 06:00 PT  
**Result:** `ok` (already current / `up_to_date` short path)

## Summary

`package.json` **0.3.1** already has remote tag **v0.3.1**, published GitHub Release, and both macOS assets (dmg+zip). No publish, no retag, no `package:mac` this run. Tip advanced via impl #5 (addon-fit) + chore dual-signal without a version bump — SoT remains package version, not tip SHA.

**Release URL:** https://github.com/kaspianseehg/Agent-Command-Locus/releases/tag/v0.3.1

## Check

| Field | Value |
|-------|--------|
| package.json version | `0.3.1` |
| tag | `v0.3.1` |
| CHECK_RESULT | `up_to_date` |
| tag_on_remote | yes |
| release_exists | yes |
| isDraft | false |
| isPrerelease | false |
| tip (origin/main at scan) | `0e90bfa1529cc31ff2e68f0e07149f97ac16a122` |
| tip CI | success (`30434052934` ci on tip) |
| open issues / PRs | 0 / 6 (Dependabot majors #3/#4/#6–#9) |
| short_path | true |
| packages_rebuilt | false |
| meta_push | pending (this dual-signal) |
| product_suite | skipped_meta_only |

```
bash scripts/release.sh --check
→ version=0.3.1 tag=v0.3.1 mode=check packages=0 draft=0
→ tag_on_remote=1 release_exists=1
→ OK: release v0.3.1 already exists
→ CHECK_RESULT=up_to_date
```

## Assets

| Asset | Size | Updated (UTC) | State |
|-------|------|----------------|--------|
| `Agent-Command-Locus-darwin-arm64.dmg` | 112,516,543 | 2026-07-22T23:47:03Z | uploaded |
| `Agent-Command-Locus-darwin-arm64.zip` | 99,995,905 | 2026-07-22T23:46:59Z | uploaded |

Both expected assets present. **No** `--with-packages` rebuild (cap: ≤1 full mac package per version; already shipped 2026-07-22).

## CHANGELOG parity

- Repo still on product version **0.3.1** (no new `##` section required this week).
- Release body from first ship remains authoritative for this tag.
- Dependabot minor merge (#5 addon-fit) did **not** bump package version → no new release.

## Actions taken

1. Heartbeat START (`job=acl-release-hygiene`)
2. `bash scripts/release.sh --check` → `up_to_date`
3. `gh release view v0.3.1` — confirmed dmg+zip + non-draft
4. Live tip + CI refresh (`0e90bfa` / run `30434052934` success)
5. No invent version bump; no delete old releases; no secrets
6. Dual-signal: this report + surgical STATUS `last_jobs.acl-release-hygiene` + meta commit
7. Concurrent dirt: none (clean tree; prior triage absorbed by impl 01:00)

## Notes

- Community draft mode does not block Releases (N/A — no release write).
- Unsigned OSS beta assets remain as previously uploaded.
- Next product release needs intentional version bump + CHANGELOG on main (impl/deps), not this job inventing one.
- Electron Option A **closed** (39.8.10 on tip); not carried as open A/B.

## ASK_*

```
ASK_HERMES:
- (carry) Dependabot majors remaining PRs #3/#4/#6-#9 (vite/xterm/react/types-node + Actions 4→7). deps-weekly owns; #8 may need rebase after #5 lockfile. No force audit fix. Safe minor #5 merged 2026-07-29.
- (carry) Optional: enable GitHub Dependabot security alerts in repo settings if still disabled.

ASK_USER:
- (none)
```

## LEARNED

- Wed 06:00 `up_to_date` short path still correct when CHECK_RESULT + dmg/zip already on release — skip `package:mac`.
- Tip SHA churn from impl/deps chore(cron) must not trigger publish; SoT is `package.json` version only.
- Clean tree after overnight impl absorb → single meta dual-signal from release job is enough (no concurrent absorb needed).
- Carry Dependabot majors one-liner only; release hygiene never merges deps PRs.
- Post-push verifier remains meta-only 4-check (paths ⊆ meta/cron/*, json.tool, heartbeat, GHA full SHA); never local npm for release short path.
