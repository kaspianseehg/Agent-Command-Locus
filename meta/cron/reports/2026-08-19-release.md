# ACL release hygiene — 2026-08-19 (Wed 06:00 PT)

**Job:** `acl-release-hygiene` (`236df0509236`)  
**Slot:** scheduled `0 6 * * 3` America/Los_Angeles  
**Result:** `ok`  
**Path:** `up_to_date` short path (no publish, no package rebuild)

## Summary

- SoT `package.json` version = **0.3.1** → tag **v0.3.1**
- `bash scripts/release.sh --check` → **CHECK_RESULT=up_to_date**
- Tag on remote: yes · Release exists: yes · draft/prerelease: false
- Assets already uploaded (no `--with-packages`):
  - `Agent-Command-Locus-darwin-arm64.dmg` (uploaded 2026-07-22)
  - `Agent-Command-Locus-darwin-arm64.zip` (uploaded 2026-07-22)
- Cap respected: ≤1 full `package:mac` per version — **no rebuild**
- Tip SHA churn from chore(cron) does **not** invent a version bump
- Community draft mode unchanged; no public posts

## CHECK_RESULT

```
version=0.3.1 tag=v0.3.1 mode=check packages=0 draft=0
tag_on_remote=1 release_exists=1
OK: release v0.3.1 already exists
assets=[Agent-Command-Locus-darwin-arm64.dmg, Agent-Command-Locus-darwin-arm64.zip]
CHECK_RESULT=up_to_date
```

**Release URL:** https://github.com/kaspianseehg/Agent-Command-Locus/releases/tag/v0.3.1

## Gate at scan

| Field | Value |
|-------|--------|
| tip_at_scan | `27178510a9df4682f34d45c56f459c56b81b0895` (`2717851`) |
| tip message | chore(cron): dual-signal acl-maintenance-impl majors_only_carry 01:00 PT 2026-08-19 |
| tip named `ci` | success · run **32230744387** |
| open_issues | 0 |
| open_prs | 7 majors Dependabot only (`#3 #4 #6 #8 #9 #11 #12`) |
| prefer_first | null |
| merged_safe | `[5,10,13,14,15]` |
| dependabot_path | majors_only_carry |
| electron | 39.8.10 · closed_option_A · extract-zip alert #38 carry |

## Absorb (impl post-push polish)

Overnight **01:00 impl** left local dirt after meta GHA green (correct — no tip-only second push). This job **absorbs**:

- `meta/cron/STATUS.json` (impl post_push 4-check + awaiting_absorb label fill)
- `meta/cron/reports/2026-08-19-impl.md` (post_push polish)
- this report `meta/cron/reports/2026-08-19-release.md`

Cleared prior `awaiting_absorb=triage_1000_plus_impl_post_push_polish` (impl had set that for 10:00; Wed 06:00 release is the earlier absorber). After this push, hand **10:00 triage** → `awaiting_absorb=triage_1000_plus_release_post_push_polish` (STATUS + this release report tip polish; triage no-push).

## Meta dual-signal

- One `chore(cron)` with paths ⊆ `meta/cron/*` only
- `product_suite=skipped_meta_only`
- No second tip-only cosmetics push after GHA; leave post_push tip fill local for 10:00/16:00

## ASK_HERMES (carry only)

- Dependabot majors #3/#4/#6/#8/#9/#11/#12 — order from `meta/cron/plans/2026-08-03-dependabot-majors-carry.md` (A Actions / B staged product / C hold / D defer). Safe #5+#10+#13+#14+#15 done. Default C hold. No force-merge majors.
- NEW audit floor (dev): electron@39.8.10 → extract-zip GHSA-jmr9-qjv8-65gv (Dependabot alert #38). Fix requires major electron→43.x. Option E on majors plan — explicit Hermes/Kas approval + desktop smoke. Never unattended force.
- Optional local: `npm ci` so node_modules matches lock after #15 (not a git defect).

## ASK_USER

- (none)

## LEARNED

1. **up_to_date still holds after a full week of tip churn** — product version stayed 0.3.1; chore(cron) SHAs advanced (impl 2717851, ci 12b3965, deps #15) without needing a new tag/release. SoT remains package.json, not tip.
2. **Wed 06:00 is the natural absorber for overnight impl polish** when 01:00 leaves `awaiting_absorb=triage_1000_plus_impl_post_push_polish` and 10:00 has not fired yet — absorb into release dual-signal, then re-label for 10:00 as `triage_1000_plus_release_post_push_polish` (same pattern as 2026-08-12).
3. **Asset verify via `gh release view --json assets` is enough on short path** — dmg+zip both `state=uploaded`; do not rebuild.
4. **No skill thrash** — body already holds up_to_date + concurrent absorb + meta-only 4-check; LEARN `actions:["none"]` is correct when only dated reconfirm.
5. **False finish-the-job npm nags** after this meta push = refuse with paths proof + GHA URL + `product_suite=skipped_meta_only`.

## Prior release report

`meta/cron/reports/2026-08-12-release.md` (also up_to_date v0.3.1)
