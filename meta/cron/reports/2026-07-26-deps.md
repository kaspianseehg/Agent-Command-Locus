# ACL deps weekly — 2026-07-26

**Job:** acl-deps-weekly  
**Started (UTC):** 2026-07-26T00:43:21Z  
**Auth:** `kaspianseehg` (gh keyring; scopes: gist, read:org, repo, workflow)  
**Repo:** https://github.com/kaspianseehg/Agent-Command-Locus  
**Local HEAD:** `bdb2e1a` (`main` = `origin/main`)  
**Mode:** LIGHT · safe patches only (≤1 PR) · majors → ASK_HERMES / plan  

## Summary

| Check | Result |
|-------|--------|
| `npm audit --omit=dev` | **0** vulnerabilities |
| `npm audit` (incl. dev) | **1 high** — `electron@34.5.8` (devDep `@acl/desktop`) |
| `npm outdated` within range | **none** (Current == Wanted for all listed pkgs) |
| Open Dependabot / `dependencies` PRs | **0** |
| `.github/dependabot.yml` | **missing** |
| Dependabot alerts API | **disabled** (403 / alerts off) |
| Safe patch PR opened | **0** (nothing in-range to bump) |
| Majors merged | **0** (policy: no blind majors) |

## Production audit

```
found 0 vulnerabilities
```

Prod dependency surface is clean. No emergency prod patch required.

## Dev audit — Electron (carry + plan)

- **Installed:** `electron@^34.3.0` → resolved **34.5.8** (latest on 34.x line)
- **Severity:** high (single npm audit meta-vuln covering many GHSA IDs)
- **Audit fix path:** `npm audit fix --force` → **electron@43.2.0** (`isSemVerMajor: true`)
- **Floor notes from prior jobs:** bulk of listed ranges clear by **≥38.8.6** / **39.8.x** class; npm’s forced recommendation is **43.2.0**
- **Not auto-shipped:** major Electron bump needs node-pty rebuild, desktop smoke, typecheck/tests, optional `package:mac` — outside “safe patch” under POLICY

### Notable GHSA themes (installed ≤39.8.4)

ASAR integrity bypass, AppleScript injection (`moveToApplicationsFolder`), SW spoof of `executeJavaScript` IPC, origin/permission handler issues, second-instance OOB read, nodeIntegrationInWorker scoping, UAF (offscreen/download/permission/PowerMonitor), Windows protocol/login-item path issues, custom protocol / webRequest header injection, USB device list validation, clipboard crash, `window.open` target scoping, undocumented `commandLineSwitches` injection.

## Outdated (latest beyond range only)

All entries below are **semver-major (or 0.x minor that is treated as major)** relative to declared ranges. **No within-range patch drift.**

| Package | Current/Wanted | Latest | Notes |
|---------|----------------|--------|-------|
| electron | 34.5.8 | 43.2.0 | **security high** — plan only |
| react / react-dom | 18.3.1 | 19.2.x | major UX/runtime |
| @types/react(‑dom) | 18.x | 19.x | track React major |
| @xterm/xterm | 5.5.0 | 6.0.0 | major terminal API |
| @xterm/addon-fit | 0.10.0 | 0.11.0 | 0.x minor |
| vite | 6.4.3 | 8.1.5 | major toolchain |
| @vitejs/plugin-react | 4.7.0 | 6.0.4 | major w/ Vite |
| typescript | 5.9.3 | 7.0.2 | major |
| @types/node | 22.20.1 | 26.x | major types |
| concurrently | 9.2.4 | 10.0.4 | major |
| wait-on | 8.0.5 | 9.1.0 | major |
| esbuild | 0.25.12 | 0.28.1 | 0.x minor |

Workspace ranges already at latest **wanted** for caret constraints — `npm update` would be a no-op for lockfile churn.

## Dependabot / GitHub

- `gh pr list --label dependencies` → empty  
- `gh pr list --search dependabot` → empty  
- No historical Dependabot PRs found in quick scan  
- Vulnerability alerts disabled on repo (API 403)  
- Enabling alerts + optional `dependabot.yml` remains **ASK_HERMES** (needs repo admin / broader scopes)

## Decision

1. **No safe patch PR** — zero in-range outdated packages; prod audit clean.  
2. **Do not** run `npm audit fix --force` unattended.  
3. **Electron security bump** filed as durable plan: `meta/cron/plans/2026-07-26-electron-security-bump.md` — needs Hermes/Coda smoke window, not light auto-merge.  
4. Other majors (React 19, Vite 8, TS 7, xterm 6) stay backlog / non-urgent unless a security advisory appears.

## Actions taken

1. Heartbeat `event=start`  
2. `npm audit --omit=dev` + full `npm audit` + `npm outdated`  
3. Dependabot / dependencies PR scan via `gh`  
4. Confirmed latest 34.x / 38.x / 39.x / 43.x Electron tags for planning floors  
5. Wrote this report + Electron plan + `STATUS.json` dual-signal  
6. Meta commit push (hygiene only; no product code)

## PR / merge

- **Opened:** 0  
- **Merged:** 0  
- **Forced audit fix:** not run  

## ASK_HERMES

- **Approve Electron security bump window** per `meta/cron/plans/2026-07-26-electron-security-bump.md`  
  - Preferred path: staged major to **39.8.10** (or audit floor ≥38.8.6) with desktop smoke, then consider **43.2.0** if still required by audit  
  - Or authorize single jump to **43.2.0** if Coda owns full smoke + package:mac  
  - Do **not** light-cron force-merge  
- Optional: enable GitHub Dependabot vulnerability alerts + add `.github/dependabot.yml` (npm weekly, PR limit 1–2, ignore major Electron/React/Vite until plan exists)  
- Optional non-security majors: React 19 / Vite 8 / TS 7 / xterm 6 — schedule only if product roadmap wants them  

## ASK_USER

- none (security Electron bump is Hermes/Coda ops unless product constraints on Electron major)
