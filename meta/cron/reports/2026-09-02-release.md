# ACL release hygiene — 2026-09-02 (Wed 06:00 PT)

**Job:** `acl-release-hygiene` (`236df0509236`)  
**Slot:** scheduled `0 6 * * 3` America/Los_Angeles  
**Result:** `ok`  
**Path:** `up_to_date` short path (no publish, no package rebuild)

## Summary

- SoT `package.json` version = **0.3.1** → tag **v0.3.1**
- `bash scripts/release.sh --check` → **CHECK_RESULT=up_to_date**
- Tag on remote: yes · Release exists: yes · draft/prerelease: false
- Assets already uploaded (no `--with-packages`):
  - `Agent-Command-Locus-darwin-arm64.dmg` (uploaded 2026-07-22, state=uploaded)
  - `Agent-Command-Locus-darwin-arm64.zip` (uploaded 2026-07-22, state=uploaded)
- Cap respected: ≤1 full `package:mac` per version — **no rebuild**
- Tip SHA churn from chore(cron) does **not** invent a version bump
- Community draft mode unchanged; no public posts
- **product_suite:** `skipped_meta_only`
- **wed_release_variant:** true (06:00 absorbs overnight 01:00 impl polish)

## CHECK_RESULT

```
version=0.3.1 tag=v0.3.1 mode=check packages=0 draft=0
tag_on_remote=1 release_exists=1
OK: release v0.3.1 already exists
assets=[Agent-Command-Locus-darwin-arm64.dmg, Agent-Command-Locus-darwin-arm64.zip]
CHECK_RESULT=up_to_date
```

**Release URL:** https://github.com/kaspianseehg/Agent-Command-Locus/releases/tag/v0.3.1

## Gate at scan

| Field | Value |
|-------|--------|
| tip_at_scan | `115c73493d1f730c024fb0f2694c64e2465f45e3` (`115c734`) |
| tip message | chore(cron): dual-signal acl-maintenance-impl majors_only_carry 01:00 PT 2026-09-02 |
| tip named `ci` | success · run **33606711150** |
| open_issues | 0 |
| open_prs | 7 majors Dependabot only (`#3 #4 #6 #8 #9 #11 #12`) |
| prefer_first | null |
| merged_safe | `[5,10,13,14,15,16]` |
| dependabot_path | majors_only_carry |
| electron | 39.8.10 · closed_option_A · extract-zip alert #38 carry |
| new_prs_this_window | [] |

## Absorb (impl post-push polish)

Overnight **01:00 impl** left local dirt after meta GHA green (correct — no tip-only second push). This job **absorbs**:

- `meta/cron/STATUS.json` (impl post_push 4-check + tip fill)
- `meta/cron/reports/2026-09-02-impl.md` (post_push polish)
- this report `meta/cron/reports/2026-09-02-release.md`

Top-level `awaiting_absorb` at scan was already null (impl cleared `triage_2200_plus_ci_post_push_polish` after its push; local `signals.awaiting_absorb` still named the 10:00 handoff). After this push, hand **10:00 triage** → `awaiting_absorb=triage_1000_plus_release_post_push_polish` (STATUS + this release report tip polish; triage no-push; 16:00 ci absorbs if still dirty).

## Meta dual-signal

- One `chore(cron)` with paths ⊆ `meta/cron/*` only
- `product_suite=skipped_meta_only`
- No second tip-only cosmetics push after GHA; leave post_push tip fill local for 10:00/16:00

## ASK_HERMES (carry only)

- Dependabot majors #3/#4/#6/#8/#9/#11/#12 — order from `meta/cron/plans/2026-08-03-dependabot-majors-carry.md` (A Actions / B staged product / C hold / D defer). Safe #5+#10+#13+#14+#15+#16 done. Default **C hold**. No force-merge majors. Light jobs = majors_only_carry absorb.
- Audit floor (dev): electron@39.8.10 → extract-zip GHSA-jmr9-qjv8-65gv (Dependabot alert #38). Fix requires major electron→44.0.0 (npm audit fix --force path). Option E on majors plan — explicit Hermes/Kas approval + desktop smoke. Never unattended force.
- Next: 10:00 triage live-list (possible prefer_first restore). Do not invent prefer_first from green majors. 16:00 ci absorbs `triage_1000_plus_release_post_push_polish` if dirt remains.

## ASK_USER

- (none)

## LEARNED

1. **up_to_date still holds after another full week of tip churn** — product version stayed 0.3.1 since 2026-07-22 ship; weekly Wed release continues short-path with dmg+zip present. SoT remains package.json, not tip SHA. `merged_safe` now includes **#16**.
2. **Wed 06:00 remains the natural absorber for overnight 01:00 impl post_push polish** when 10:00 has not fired yet — absorb STATUS + `*-impl.md`, then re-label `awaiting_absorb=triage_1000_plus_release_post_push_polish` for 10:00 no-push / 16:00 ci.
3. **Asset verify via `gh release view --json assets` is enough on short path** — both assets `state=uploaded`; do not rebuild; cap ≤1 package:mac per version holds.
4. **No skill thrash** — cron-orchestration already encodes up_to_date + concurrent absorb + meta-only 4-check + electron flake recovery; LEARN `actions:["none"]` is correct for dated reconfirm only.
5. **False finish-the-job npm nags** after this meta push = refuse with paths proof + GHA URL + `product_suite=skipped_meta_only`.

## Prior release report

`meta/cron/reports/2026-08-26-release.md` (also up_to_date v0.3.1)

### Meta-only 4-check (post-push)

- **paths:** ⊆ `meta/cron/*` (STATUS + 2026-09-02-impl polish + 2026-09-02-release)
- **json.tool + semantic asserts:** pending until push (gate tip `115c734`)
- **heartbeat:** START (SUCCESS after push)
- **GHA:** pending on this chore SHA
- **product_suite:** `skipped_meta_only`
- **note:** post_push tip polish left **local dirt** for 10:00 triage / 16:00 ci absorb — no tip-only second push
