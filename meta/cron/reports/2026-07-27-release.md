# ACL release hygiene — 2026-07-27

**Job:** `acl-release-hygiene` (`236df0509236`)  
**At:** 2026-07-27T13:05:34Z  
**Slot:** Wed 06:00 PT  
**Result:** `ok` (already current)

## Summary

`package.json` **0.3.1** already has remote tag **v0.3.1**, published GitHub Release, and both macOS assets. No publish, no retag, no `package:mac` this run.

**Release URL:** https://github.com/kaspianseehg/Agent-Command-Locus/releases/tag/v0.3.1

## Check

| Field | Value |
|-------|--------|
| package.json version | `0.3.1` |
| tag | `v0.3.1` |
| CHECK_RESULT | `up_to_date` |
| tag_on_remote | yes |
| release_exists | yes |
| isDraft | false |
| isPrerelease | false |
| tip (local main) | `039a19c` |
| tip CI | success (`30264292064` deps dual-signal) |
| open issues / PRs | 0 / 0 |

```
bash scripts/release.sh --check
→ version=0.3.1 tag=v0.3.1 mode=check packages=0
→ tag_on_remote=1 release_exists=1
→ CHECK_RESULT=up_to_date
```

## Assets

| Asset | Size | Updated (UTC) | State |
|-------|------|----------------|--------|
| `Agent-Command-Locus-darwin-arm64.dmg` | 112,516,543 | 2026-07-22T23:47:03Z | uploaded |
| `Agent-Command-Locus-darwin-arm64.zip` | 99,995,905 | 2026-07-22T23:46:59Z | uploaded |

Both expected assets present. **No** `--with-packages` rebuild (cap: one full mac package per version; already shipped on first publish).

## CHANGELOG parity

- Repo `CHANGELOG.md` has `## 0.3.1 — 2026-07-22` (Added skins/OpenClaude/media + Fixed Electron bundle/bootstrap).
- Release body matches that section. No notes refresh required.

## Actions taken

1. Heartbeat START (`job=acl-release-hygiene`)
2. `bash scripts/release.sh --check` → `up_to_date`
3. `gh release view v0.3.1` — confirmed assets + body
4. No invent version bump; no delete old releases; no secrets
5. Dual-signal: this report + STATUS update (+ meta commit if tree needs it)

## Notes

- Community draft mode does not block Releases (N/A — no release write).
- Unsigned OSS beta assets remain as previously uploaded.
- Concurrent `acl-ci-health` heartbeat observed same minute; coalesce meta push if sibling lands first.
- Next product action for a new release is an intentional version bump + CHANGELOG section on main (impl/deps path), not this job inventing one.

## LEARNED

- Weekly Wed 06:00 short path is correct when `CHECK_RESULT=up_to_date` + dmg/zip already on the release — skip heavy `package:mac`.
- Do not re-upload assets “for freshness” on every weekly tick; first-of-version only.
- Tip can advance via deps/impl chore(cron) without a new product version — release hygiene must key off `package.json` version, not tip SHA churn.
- Carry Electron ASK from STATUS; release job does not own majors.
- Local `dist-package` leftovers are fine; presence of GH assets is the gate, not wiping local artifacts.

```
ASK_HERMES:
- (carry unchanged) Electron security: Option A (→39.8.10 / ≥38.8.6) or B (→43.2.0) per meta/cron/plans/2026-07-26-electron-security-bump.md
- (carry unchanged) optional: enable Dependabot vulnerability alerts + .github/dependabot.yml

ASK_USER:
- (none)
```

## Dirt absorb

- Absorbed concurrent no-push `meta/cron/reports/2026-07-27-triage.md` into this dual-signal commit (triage no-push policy).
- Also absorbed concurrent local `2026-07-27-ci-health.md` (ci-health meta_push=skipped_clean_green).


## Dual-signal close

| Field | Value |
|-------|--------|
| meta commit (absorb) | `e27562a` |
| GHA on absorb HEAD | success [30268830321](https://github.com/kaspianseehg/Agent-Command-Locus/actions/runs/30268830321) |
| product_suite | skipped_meta_only |
| STATUS fix | follow-up commit restores `last_jobs.acl-release-hygiene` → `2026-07-27-release.md` after concurrent STATUS clobber |
