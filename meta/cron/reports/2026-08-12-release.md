# ACL release hygiene — 2026-08-12

**Job:** `acl-release-hygiene` (`236df0509236`)  
**At:** 2026-08-12T13:00:30Z  
**Slot:** Wed 06:00 PT  
**Result:** `ok` (already current / `up_to_date` short path)

## Summary

`package.json` **0.3.1** already has remote tag **v0.3.1**, published GitHub Release, and both macOS assets (dmg+zip). No publish, no retag, no `package:mac` this run. Tip advanced via overnight majors-only impl dual-signal (`088e47d`) without a version bump — SoT remains package version, not tip SHA. Still **0.3.1** after a full week of majors_only plateau + prior safe merges (#5/#10/#13/#14).

**Release URL:** https://github.com/kaspianseehg/Agent-Command-Locus/releases/tag/v0.3.1

## Check

| Field | Value |
|-------|--------|
| package.json version | `0.3.1` |
| tag | `v0.3.1` |
| CHECK_RESULT | `up_to_date` |
| tag_on_remote | yes |
| release_exists | yes |
| isDraft | false |
| isPrerelease | false |
| release createdAt | 2026-07-22T22:52:17Z |
| tip (origin/main at scan) | `088e47d0ac03c7b65bac21d5216c2bbfbd0a4358` |
| tip CI | success (`31576595319` named `ci` on tip) |
| open issues / PRs | 0 / 7 (Dependabot majors #3/#4/#6/#8/#9/#11/#12) |
| short_path | true |
| packages_rebuilt | false |
| meta_push | pushed _(filled after push)_ |
| product_suite | skipped_meta_only |
| absorbed | impl post-push polish (`STATUS.json` + `2026-08-12-impl.md`) |

```
bash scripts/release.sh --check
→ version=0.3.1 tag=v0.3.1 mode=check packages=0 draft=0
→ tag_on_remote=1 release_exists=1
→ OK: release v0.3.1 already exists
→ assets: Agent-Command-Locus-darwin-arm64.dmg, Agent-Command-Locus-darwin-arm64.zip
→ CHECK_RESULT=up_to_date
```

## Assets

| Asset | State | size |
|-------|--------|------|
| `Agent-Command-Locus-darwin-arm64.dmg` | uploaded (since 2026-07-22) | 112516543 |
| `Agent-Command-Locus-darwin-arm64.zip` | uploaded (since 2026-07-22) | 99995905 |

Both expected assets present via `gh release view v0.3.1`. **No** `--with-packages` rebuild (cap: ≤1 full mac package per version; already shipped 2026-07-22).

## CHANGELOG parity

- Repo still on product version **0.3.1** (no new `##` section required this week).
- Release body from first ship remains authoritative for this tag.
- Safe deps merges (#5 addon-fit, #10 tsx, #13 postcss, #14 tsx+ws) and cron tip churn did **not** bump package version → no new release.

## Actions taken

1. Heartbeat START (`job=acl-release-hygiene` slot=scheduled_0600_PT)
2. `bash scripts/release.sh --check` → `up_to_date`
3. `gh release view v0.3.1` — confirmed dmg+zip + non-draft + sizes
4. Live tip + CI refresh (`088e47d` / run `31576595319` success) + open PR list (7 majors)
5. No invent version bump; no delete old releases; no secrets
6. Absorb overnight dirt: impl post-push polish on STATUS + `2026-08-12-impl.md` (never discard)
7. Dual-signal: this report + surgical STATUS `last_jobs.acl-release-hygiene` + one meta commit
8. Carry ASK/plans one-liners only (majors → deps-weekly); Electron closed_option_A

## Notes

- Community draft mode does not block Releases (N/A — no release write).
- Unsigned OSS beta assets remain as previously uploaded.
- Next product release needs intentional version bump + CHANGELOG on main (impl/deps), not this job inventing one.
- Electron Option A **closed** (39.8.10 on tip); not carried as open A/B.
- Prior release job report was 2026-08-05; this run refreshes `last_jobs.acl-release-hygiene.report` to this week's path.
- After this push, leave own post_push cosmetic dirt for 10:00 triage no-push → 16:00 ci (`awaiting_absorb` may become `triage_1000_plus_release_post_push_polish`).

## ASK_*

```
ASK_HERMES:
- (carry) Dependabot majors #3/#4/#6/#8/#9/#11/#12 — order from meta/cron/plans/2026-08-03-dependabot-majors-carry.md (A Actions / B staged product / C hold / D defer). Safe #5+#10+#13+#14 done. Live 06:00 2026-08-12: tip 088e47d green (01:00 impl meta); prefer_first=null; new_prs=[]; #11 PR-branch FAILURE = non_ci_noise; majors_only_carry. No force-merge majors.
- (carry) Optional local: npm ci so node_modules matches lock after #5/#10/#13/#14 (not a git defect).

ASK_USER:
- (none)
```

## Post-push meta-only 4-check

| Check | Result |
|-------|--------|
| paths ⊆ meta/cron/* | **pending_fill** after push |
| python3 -m json.tool STATUS.json + semantics | **pending_fill** after push |
| heartbeat START + SUCCESS | **pass** START; SUCCESS after push |
| gh run list --commit full SHA = success | **pending_fill** after push |
| product_suite | skipped_meta_only |

**post_push_head:** _(filled after GHA)_  

Local STATUS/report tip polish left as dirt for next absorber (10:00 triage no-push or 16:00 ci).

## LEARNED

- Wed 06:00 `up_to_date` short path still correct when CHECK_RESULT + dmg/zip already on release — skip `package:mac` even after multi-week tip SHA churn with no package.json bump (reconfirmed 2026-08-12 after 2026-08-05).
- Overnight 01:00 impl often leaves local post_push polish dirt (STATUS + impl report); scheduled 06:00 release is a valid absorber when tree is dirty — never `git checkout --` discard.
- SoT remains `package.json` version; majors-only carry nights + safe lockfile merges never create a release without an intentional version bump.
- Surgical STATUS merge only for `last_jobs.acl-release-hygiene` + shared tip/release pointers; full-file rewrite would clobber sibling last_jobs.
- Post-push verifier remains meta-only 4-check (paths ⊆ meta/cron/*, json.tool, heartbeat, GHA full SHA); never local npm for release short path.
- Skill/prompt already hold this path → LEARN actions:["none"] is correct when only dated reconfirm lands.
